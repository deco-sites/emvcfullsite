import Header, {
  HeaderProps,
} from "deco-sites/emvcfullsite/components/header/Header.tsx";

function HeaderSection(props: HeaderProps) {
  return <Header {...props} />;
}

export default HeaderSection;
